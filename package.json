{
  "name": "sentinel",
  "version": "1.0.0",
  "private": true,
  "description": "AI-powered task scheduler - schedule tasks that run via OpenCode agents",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w packages/core && npm run build -w packages/cli && npm run build -w packages/desktop",
    "build:core": "npm run build -w packages/core",
    "build:cli": "npm run build -w packages/cli",
    "build:desktop": "npm run build -w packages/desktop",
    "dev": "npm run dev -w packages/desktop",
    "cli": "npm start -w packages/cli"
  },
  "engines": {
    "node": ">=18"
  }
}
